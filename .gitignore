# Rust
target/
# Node
node_modules/
dist/
# `just wasm`'s output copy (rebuilt from crates/vpet-abi; see justfile)
hosts/web/public/*.wasm
# Playwright output
hosts/web/test-results/
hosts/web/playwright-report/
# Python tooling
.venv/
__pycache__/
*.pyc
# Generated art previews and concept images (large, reproducible)
assets/**/preview/
assets/**/concept/
# `spritekit gen`'s per-round log (docs/art/ART_PIPELINE.md); a local working artifact, not a
# durable asset.
assets/**/gen-log.jsonl
# Local config
.env
.env.*
# OS
.DS_Store

# design-sync (docs: .design-sync/NOTES.md); staged converter, its deps, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
packages/vpet-ds/src/sprites.gen.ts
