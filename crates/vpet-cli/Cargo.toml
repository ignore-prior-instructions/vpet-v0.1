[package]
name = "vpet-cli"
version = "0.1.0"
edition.workspace = true

[[bin]]
name = "vpet-cli"
path = "src/main.rs"

[dependencies]
vpet-core = { path = "../vpet-core" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
